
public class FloorStateFactory
{
    private NewFloorManager _ctx;
    public FloorStateFactory(NewFloorManager ctx)
    {
        _ctx = ctx;
    }

    //public BattleStateMachine BattleState() 
    //{
    //    return new BattleStateMachine(_ctx);
    //}
}
